interface:
  display_name: 'Paxaver Parent'
  short_description: 'Lunch, wallet, events, and student info for Paxaver families.'
  brand_color: '#3B82F6'
  default_prompt: 'Order lunch for my child today.'

policy:
  allow_implicit_invocation: true

dependencies:
  tools:
    - type: 'mcp'
      value: 'paxaver'
      description: 'Paxaver MCP server'
      transport: 'streamable_http'
      url: 'https://mcp.paxaver.com/mcp'
